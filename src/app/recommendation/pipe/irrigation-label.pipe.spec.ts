import { IrrigationLabelPipe } from './irrigation-label.pipe';

describe('IrrigationLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new IrrigationLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
