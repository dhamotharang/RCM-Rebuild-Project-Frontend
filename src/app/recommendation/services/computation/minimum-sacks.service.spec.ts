import { TestBed } from '@angular/core/testing';

import { MinimumSacksService } from './minimum-sacks.service';

describe('MinimumSacksService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: MinimumSacksService = TestBed.get(MinimumSacksService);
  //   expect(service).toBeTruthy();
  // });
});
